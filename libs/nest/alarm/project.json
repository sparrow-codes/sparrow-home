{
  "name": "alarm",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/nest/alarm/src",
  "projectType": "library",
  "tags": ["scope: be-alarm"],
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/nest/alarm/jest.config.ts"
      }
    }
  }
}
